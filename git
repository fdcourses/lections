git init - создать локальный репозиторий

git add - индексирует файл или файлы

git commit - создает коммит
git commit -m 'Text'
git commit --amend - изменить последний комит

git log - показывает историю комитов
--pretty=(short || medium || full ||fuller || oneline)

git diff - изменение в файлах
git diff --staged - изменения в проиндексированных файлах

git reflog - просмотр действий в локально репозитории

git restore - восстанавливает файл до поледней закомиченной версии

git restore --staged - убрать файл из индексации

git rm - удаляет файл из отслеживаемых

git rm --cached - удалит из индекса а файл оставит


